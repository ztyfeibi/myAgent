# 2026-08-31

## TASK-001 文档整理：从执行日志改为已完成任务记录（第一步）

用户要求只整理 `docs/task/TASK-001-rag-extension-foundation.md`，**不动代码**。10 个步骤全部完成。

### 固定的事实来源（全部实测/git 核验）

| 项 | 值 |
|---|---|
| deer-flow HEAD | `a0eead4e build(extensions): register RAG extension snapshot`，工作区干净 |
| rag-extension HEAD | `d034a3a`，工作区干净 |
| 上游基线 | `788a890b`，自其起 5 个提交：`602ecb82` → `848e1157` → `fdadff7b` → `a29a83be` → `a0eead4e` |
| 六文件 | 62 passed |
| 包内 | 7 passed |
| Gateway 回归 | 138 passed |
| 综合定向回归 | **311 passed，0 失败**（单次运行实测） |
| uv 锁 | `uv 0.11.1 lock --project backend --check` EXIT 0，249 packages |
| Tenki | 已在 `a29a83be` 整体删除，10 files / 2047 删除；仅 CHANGELOG 保留历史 |
| 开发源聚合 SHA256 | `b173fdd2…`，与文档记录一致；快照与开发源 `diff -rq` 无差异 |

**综合定向回归 311 = 六文件 62 + test_gateway_services 138 + test_app_config_reload 38 + test_extension_config 10 + test_extension_app_loading 8 + test_extension_loader 39 + test_extension_registry 16。**

### ⚠️ 未解决：257 口径对不上

用户给的综合回归数字是 `257 passed`，但我这边**复现不出来**。实测分片：62 / 138 / 38 / 10 / 8 / 39 / 16 / config_version 9+1环境失败。唯一能凑出 257 的是 62+138+10+8+39（恰好漏掉 `test_app_config_reload` 与 `test_extension_registry`），不像是有意选择。

文档 §0 保留了**可复现的 311**，并在下方加了「待确认口径」注记说明 257 无法复现。**等用户给出 257 的具体文件集合后再修正这一格。**

### 文档结构改动（794 → 1025 行）

- 顶部状态摘要改为统一终态：`状态：已完成并验收 / 正式验收 7/7 / RAG 测试 62/62 / 扩展包测试 7/7 / 当前实现 Stub RAG 基础闭环`，并列出最终提交号与权威源码位置
- 新增 `§0 最终验证结果`（验证表 + 各范围文件清单口径 + 不相加声明）
- 新增 `§2.1 最终实现范围`（8 项：显式双模式 / KnowledgeModeMiddleware / Stub knowledge_search / Evidence 契约 / 临时 Ledger / Trace 可观察 / general 保持原生工具集 / 非法模式失败关闭）
- `§3` 改写为「明确未实现（非 TASK-001 范围）」：真实 BM25/Vector/KG 检索、RRF 与 Reranker、LLM Grading、Evidence Sufficiency 循环、RagGuard、MCP 外部证据、文档摄取索引、生产级数据库
- `§5` 加口径统一说明 + 7 项验收状态表：**明确 Gateway 的 test_01–test_08 是「8 个运行场景」而非「8 项验收」**
- `§6` 改为「决策记录（均已落地，无未决阻塞）」；`§7.4` 改为「历史遗留问题（均已解决或已转为已知限制）」
- 新增 `§7.14 TASK-001 提交链`
- `§7.13` 更新为 Tenki 最终状态（提交 `a29a83be` + 核验表）
- **新增 `§9 历史执行记录`**：把 §7.10 事故与 §7.11 待办清单整体搬过来（改号为 9.1 / 9.2，⏳ 全部改 ✅，待办清单改为「结果」表）
- 新增 `§10 安装与源码关系`、`§11 最终已知限制`（6 项，含 pytest 的 `py` 依赖观察项）

### 仍然存在的问题（§11）

`.pytest_cache` 权限警告 / 本机全量后端测试不适合当验收门槛（`wsl.exe` 黑名单 + GBK stderr 导致 `result.stderr is None`）/ 必须钉版 uv 0.11.1 / Evidence grounding 未由 RagGuard 强制 / 本地 config.yaml 不入库 / pytest 的 `py` 依赖缺口待用户决定是否单开任务。

### 下一步

单独编写 `CURRENT-STATE.md`（用户明确说「完成这一步后…然后再单独编写」）。

---

## 第二步：编写 docs/CURRENT-STATE.md（当前实现清单）

产出：`D:\Dev\DevProjects\AI\myAgent\docs\CURRENT-STATE.md`。目标——只描述代码现在真实具备什么，不混入未来架构。

### 做法

先逐文件核对事实（Read 8 个模块 + 2 个包内测试 + config.yaml + pyproject + uv.lock + .gitignore），再用全仓 grep 交叉验证，最后按 12 步规范落笔。全文 12 节 + 附录，每节末尾对照「完成标准」自检。

### 口径体系（可复用）

定义了 7 种状态口径，**禁止**使用「支持/完成/已具备」等无界定词：
1 已实现并验证 / 2 已实现但仅单元测试 / 3 已接线但未运行验证 / 4 保留 DeerFlow 原生能力 / 5 Stub 临时实现 / 6 未实现 / 7 不在当前范围

### 核对出的关键事实（新发现，此前未记录）

1. **`config.yaml` 是 gitignored**（`.gitignore:33`），RAG 运行时接线（`extensions.middlewares` + `plugins` 块，第 1582-1592 行）**只存在于本机，不入库**；`config.example.yaml` 里**没有**对应块 → 克隆者装完 extension 不会自动启用 RAG。
2. **`rag_mode` 在 `backend/app/` 与 `backend/packages/` 中 grep 零命中** → Gateway 完全不认识该字段，**不做边界校验**。非法模式是运行期中间件抛 `RagModeError` → run 失败（`RunStatus.error`），**不是 HTTP 422**。
3. **`README.md:56` 文档错误**：声称「Invalid values are rejected with HTTP 422 at the run boundary」，代码里没有任何 422 逻辑。已记为待决项，本次未改。
4. **TASK-001 入库的生产改动只有 3 个文件**：`.gitignore`(+4)、`backend/pyproject.toml`(+5/-1)、`backend/uv.lock`(+25/-1)。这是「原生能力未改动」最硬的证据。
5. **三份副本逐字节相同**：`rag-extension/` 源码 == `backend/extensions/sources/rag-extension/` 快照 == `.venv/Lib/site-packages/rag_extension/`（8 个模块 `diff -q` 全 SAME）。
6. **冻结是「每层构造时冻结自己直接拥有的一层」**：`_frozen_mapping` = `MappingProxyType(dict(value))` 是浅拷贝。`test_evidence_provenance_nested_filter_is_not_item_mutable` 能通过，是因为传入的 `request.metadata_filter` 已被 `RetrievalRequest.__post_init__` 冻结过；直接传裸嵌套 dict 则内层仍可变。
7. **残留垃圾**：`backend/extensions/sources/.rag-extension.remove-3ubzl2z9/`（某次 `extension-remove` 中断残留，gitignored，不影响运行）。
8. **pyproject/uv.lock 指向 gitignored 路径** → 干净 clone 后不能直接 `uv sync --locked`，推断需先 `make extension-install` 再 sync。**该重建路径未实测**。

### general 模式三层区分（用户点名要求）

- 层一 架构未改：✅ 只新增中间件，生产代码零改动，`test_01`/`test_08` 实证 `bound[0] == ["native_tool"]`
- 层二 有自动化回归：✅ 12+ 用例覆盖，**但所有用例的「原生工具」是同一个 1 行桩件** `native_tool`
- 层三 真实 MCP/Skill/Sandbox/HTTP-SSE 运行验证：❌ **未做**，如实标记为未验证

### 定稿复跑

六个 RAG 文件 `62 passed in 2.47s`（exit 0）。

### 遗留待决（3 项，已写入文档附录）

① README 422 措辞修正；② 是否给 `config.example.yaml` 加 RAG 注释块；③ 删除残留目录。另：干净环境重建路径建议单开任务验证。

---

## 第三步：As-Is 架构（追加到 docs/CURRENT-STATE.md 第二部分 §13–§23）

文档从 476 行扩到 1022 行，拆成两部分：第一部分 §0–§12（能力清单，第二步产物）+ 第二部分 §13–§23（As-Is 架构，本次新增）。

### 新增章节

§13 架构边界（4 条硬约束）/ §14 组件关系图 / §15 组件所有权（14 项）/ §16 三条模式时序（general / knowledge / 非法）/ §17 Evidence 数据流（每节点标实现模块）/ §18 Trace 与持久化边界（三类数据分界）/ §19 安装与运行关系（四段链路）/ §20 原生能力复用标记 / §21 未实现清单（4 类 20 项）/ §22 与原设计差距对照 / §23 完成标准自检

### 对照原架构文档的关键结论

基准 `docs/architecture/01-system-design.md`（Agentic RAG v1.1）。原设计 §7 组件树实为 **10 个顶层组件**（不是 12）：
- 完全落地 2：ModePolicy、EvidenceLedger（基础版）
- 部分落地 2：RagToolFacade（4 工具只实现 knowledge_search）、RetrievalService（Stub）
- 未落地 6：DocumentReadService、ExternalEvidenceService、EvidenceSufficiencyService、RagGuardMiddleware、CitationService、RetrievalTraceRecorder

文档里特别加了防误读说明：不能把「2 完全 + 2 部分」读成「RAG 完成一半」——落地的是外壳与契约，决定检索质量的组件一个都没落地。

### 两个新补的架构事实

1. **Evidence 没有独立存储**：以 JSON 字符串躺在 `ToolMessage.content` 里，因此天然随 Checkpoint 持久化，也天然被 Trace 记录。所以三类数据（Agent 消息状态 / Tool-Evidence / Run Event）其实只有两个物理落点。
2. **当前不是固定两步 RAG Pipeline**：是否调用 `knowledge_search`、调用几次，完全由 LLM 决定。原设计 §8 的 `evaluate sufficiency` → `sufficient / search_again / abstain` 三分支控制流**不存在**。测试里的「调一次工具再回答」是 `GenericFakeChatModel` 脚本化响应，不是系统强制行为。

### 踩坑

1. **bash heredoc 写大段含 box-drawing/§/反引号的中文内容会挂**：报 `unexpected EOF while looking for matching ''`。改用 Write 工具落临时文件 + python 拼接，稳定。
2. **追加新部分会撞号**：第一部分已有 §12 完成标准自检，第二部分从 §12 起编号冲突。解决：倒序批量顺延标题（22→23 … 12→13），再批量顺延交叉引用。
3. **章节引用必须消歧**：第二部分里「§8」既可能指原架构文档也可能指本文。加了「引用约定」行，并把原设计出处列全部加「原设计」前缀，用正则复查残留。

---

## 环境垃圾清理（用户指令：先把环境垃圾清了）

### 清理前的核验（关键，避免误删）

1. **残留目录不是"看起来那样"**：`extensions/sources/.rag-extension.remove-3ubzl2z9/source/rag_extension/` 含完整源码副本，8 个模块里有 **6 个与权威源码不同**。逐字节比对发现它是**旧版本**（`contracts.py` 231 行 vs 权威 291 行，缺 2.8 阶段加的冻结语义 `__getstate__`/`__setstate__`）。删除前必须做这一步——如果它是新版本，删了就丢代码。
2. **`.stabilize/` 不是垃圾**：它是事故现场快照（381KB），含 `git-diff-deerflow.patch`(16K)、`MANIFEST.md`、`recovered/gateway`。验证过 `recovered/test_rag_extension_gateway.py` 与入库版逐字节相同、源码副本仅 `.pyc` 有差异 → 技术冗余，但是**事故恢复过程的唯一现场证据**。建议长期保留，未删。
3. **`.tmp-uv-tool/` 绝对不能碰**：钉版 uv 0.11.1 在此，DeerFlow 相关工作的必需工具（本机默认 0.11.11 不可用）。

### 已清理（11 项）

- `extensions/sources/.rag-extension.remove-3ubzl2z9/`（旧版本残留，已验证）
- `backend/.pytest_cache`、`backend/.ruff_cache`
- `rag-extension/.pytest_cache`、`.ruff_cache`
- 7 处 `__pycache__/`（不含 `.venv`）
- `.tmp-uv-resync.log` / `.tmp-uv-resync2.log`
- `.stabilize/uv-cache/`

### 删除方式的坑

- `Add-Type -AssemblyName Microsoft.VisualBasic`（回收站 API）→ **被安全策略拦**：`Add-Type compiles and loads .NET code at runtime`
- `New-Object -ComObject Shell.Application`（回收站 COM）→ **被安全策略拦**：`COM object instantiation can run arbitrary code`
- 最终用 bash `rm -r` **精确路径、不用 `-f`、不用通配符**，先删一个验证父目录完好再继续。
- **虚惊一场**：删 `__pycache__` 时三个报 `No such file or directory`（该命令触发了 `Sandbox bypassed (escalation-approved)`，沙箱视图差异）。立即 `git status` + `ls` 复查，确认全干净、六个测试文件字节数不变，是视图差异不是误删。

### 清理后验证

六个 RAG 测试 **62 passed**；rag-extension 包内 **7 passed**（用 backend venv 跑 `../../rag-extension/tests/test_plugin.py test_entry_point.py`，注意直接给目录 `../rag-extension/tests` 收集不到用例，必须给具体文件）；两仓库 `git status` 均干净。

### 文档同步

`CURRENT-STATE.md` §1.4 改为「环境残留物（已清理）」清单 + 保留项说明；附录待决项从 3 条扩到 5 条（新增 `docs/` 无版本控制、干净 clone 重建路径未实测），第 3 条标记 ✅ 已清理。
